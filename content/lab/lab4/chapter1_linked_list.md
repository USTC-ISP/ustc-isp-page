---
title: 章节一：链表基础
weight: 41
draft: true
---

## 1.1 链表的基本概念

在计算机科学中，链表是一种物理存储单元上非连续、非顺序的存储结构。与数组不同，链表中的元素在内存中不要求连续存放，而是通过“指针”把节点串起来。

链表由一系列**节点（Node）**组成，节点在运行时动态生成。每个节点通常包含两部分：

1. 数据域：存储实际数据（例如本实验的 `key` 和 `value`）。
2. 指针域：存储下一个节点（或上一个节点）的地址。



